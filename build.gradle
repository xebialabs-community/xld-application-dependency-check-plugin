apply plugin: 'java'

xlDeployServerLib = new File(new File(xlDeployServerHome), 'lib').path

repositories {
	flatDir {
		dirs xlDeployServerLib
	}
}

dependencies {
	compile ':guava:16+'
	compile ':udm-plugin-api:4.0+'
}
